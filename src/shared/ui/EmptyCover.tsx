/**
 * Заглушка на месте отсутствующей обложки: сплошная подложка и приглушённый
 * знак bloom поверх. Один в один `Cover.placeholder` из bloom-phone (доля 34%,
 * прозрачность .16) — и разделение оттуда же: знак bloom стоит у ЛЮБОЙ пустой
 * обложки (трек, плейлист, альбом, артист), а нота/винил/человечек остаются
 * только иконками РАЗДЕЛОВ («Все треки», вкладки поиска, пилюля источника).
 * Единственное исключение — аватар самого пользователя: там человечек
 * (`EmptyAvatar`), иначе лицо читалось бы как «трек без обложки».
 *
 * Заливка — `--cover-empty`: цвет блока, поднятый долей плёнки. Просто `--card`
 * не годится — заглушка лежит на блоке того же цвета и сливалась бы с ним,
 * оставляя висеть один знак без площадки.
 *
 * Масштабируется по контейнеру (100% × 100%), поэтому одинаково годится и
 * сайдбару (16px), и сетке (~140px), и hero (110px).
 */
export const EmptyCover = () => (
  <div className="cover-empty" aria-hidden="true">
    <span className="cover-empty-mark" />
  </div>
)
