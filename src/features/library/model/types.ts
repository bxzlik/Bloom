/**
 * Режим библиотеки — что отображается в правой части.
 */
export type LibMode = 'all' | 'fav' | 'history' | 'pl' | 'folder'

/** Системный (built-in) элемент сайдбара. */
export type SysItem = 'all' | 'fav' | 'history'
