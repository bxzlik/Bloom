{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the now-playing overlay (island) window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
